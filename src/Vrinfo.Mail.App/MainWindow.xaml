<Window x:Class="Vrinfo.Mail.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="VRINFO Mail"
        Icon="pack://siteoforigin:,,,/Assets/app.ico"
        Width="1280" Height="800" MinWidth="960" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Closing="OnClosing"
        Opacity="0"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display">
  <Grid>
  <DockPanel>
    <Border DockPanel.Dock="Top" Background="{DynamicResource Blue}" Padding="18,12">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="VRINFO Mail" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource HeaderFg}" VerticalAlignment="Center"/>
          <TextBlock Text="  ·  leitor IMAP" Foreground="{DynamicResource HeaderSub}" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal">
          <Button Style="{StaticResource BtnPrimary}" Command="{Binding ComposeCommand}" Margin="0,0,8,0" MinWidth="118" Padding="14,8">
            <StackPanel Orientation="Horizontal">
              <Grid Width="16" Height="16" Margin="0,0,8,0" Visibility="{Binding IsOpeningCompose, Converter={StaticResource BoolToVis}}">
                <Ellipse Stroke="#66FFFFFF" StrokeThickness="2"/>
                <Ellipse Stroke="White" StrokeThickness="2" StrokeDashArray="12 8" RenderTransformOrigin="0.5,0.5">
                  <Ellipse.RenderTransform>
                    <RotateTransform Angle="0"/>
                  </Ellipse.RenderTransform>
                  <Ellipse.Style>
                    <Style TargetType="Ellipse">
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOpeningCompose}" Value="True">
                          <DataTrigger.EnterActions>
                            <BeginStoryboard>
                              <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                 From="0" To="360" Duration="0:0:0.7"/>
                              </Storyboard>
                            </BeginStoryboard>
                          </DataTrigger.EnterActions>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Ellipse.Style>
                </Ellipse>
              </Grid>
              <TextBlock VerticalAlignment="Center" FontWeight="SemiBold">
                <TextBlock.Style>
                  <Style TargetType="TextBlock">
                    <Setter Property="Text" Value="Novo"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding IsOpeningCompose}" Value="True">
                        <Setter Property="Text" Value="Abrindo…"/>
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </TextBlock.Style>
              </TextBlock>
            </StackPanel>
          </Button>
          <Button Content="Atualizar" Style="{StaticResource BtnOnHeader}" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
          <Button Content="{Binding ThemeToggleLabel}" Style="{StaticResource BtnOnHeader}" Command="{Binding ToggleThemeCommand}" Margin="0,0,8,0"/>
          <CheckBox Content="Iniciar com o Windows" Foreground="{DynamicResource HeaderFg}" VerticalAlignment="Center"
                    IsChecked="{Binding StartWithWindows}"/>
        </StackPanel>
      </Grid>
    </Border>
    <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource StatusBg}" Foreground="{DynamicResource Text}">
      <StatusBarItem>
        <TextBlock Text="{Binding Status}"/>
      </StatusBarItem>
      <StatusBarItem HorizontalAlignment="Right">
        <ProgressBar Width="160" Height="8" IsIndeterminate="{Binding IsBusy}" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
      </StatusBarItem>
    </StatusBar>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="280"/>
        <ColumnDefinition Width="5"/>
        <ColumnDefinition Width="380"/>
        <ColumnDefinition Width="5"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <DockPanel Background="{DynamicResource Panel}">
        <TextBlock DockPanel.Dock="Top" Text="Pastas" FontWeight="SemiBold" Margin="12,12,12,4"/>
        <ListBox ItemsSource="{Binding Folders}" SelectedItem="{Binding SelectedFolder}" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
          <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
              <Setter Property="Padding" Value="6,8"/>
              <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
              <Setter Property="SnapsToDevicePixels" Value="True"/>
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd" Padding="{TemplateBinding Padding}" CornerRadius="10" Background="Transparent">
                      <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsSelected" Value="True">
                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ListSelected}"/>
                      </Trigger>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ListHover}"/>
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </ListBox.ItemContainerStyle>
          <ListBox.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal" Margin="2">
                <Border Width="52" Height="52" CornerRadius="14" Background="{DynamicResource IconPlate}" Margin="0,0,12,0">
                  <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.55" Color="White"/>
                  </Border.Effect>
                  <Image Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center"
                         Source="{Binding Logo}" Stretch="Uniform"
                         RenderOptions.BitmapScalingMode="LowQuality" RenderOptions.CachingHint="Cache"/>
                </Border>
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"
                           Foreground="{DynamicResource Text}"/>
                <TextBlock Text="{Binding Unread, StringFormat=' ({0})'}" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </DockPanel>
      <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{DynamicResource Line}"/>
      <DockPanel Grid.Column="2" Background="{DynamicResource Panel}">
        <StackPanel DockPanel.Dock="Top" Margin="8">
          <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6"/>
          <WrapPanel>
            <CheckBox Content="Não lidos" IsChecked="{Binding UnreadOnly}" Margin="0,0,8,4"/>
            <CheckBox Content="Anexos" IsChecked="{Binding AttachmentsOnly}" Margin="0,0,8,4"/>
            <CheckBox Content="Hoje" IsChecked="{Binding TodayOnly}" Margin="0,0,8,4"/>
            <CheckBox Content="Prioridade" IsChecked="{Binding HighPriorityOnly}" Margin="0,0,8,4"/>
            <CheckBox Content="Fiscal" IsChecked="{Binding FiscalOnly}" Margin="0,0,8,4"/>
          </WrapPanel>
          <WrapPanel>
            <Button Content="Lixeira" Style="{StaticResource BtnGhost}" Command="{Binding DeleteSelectedCommand}" Margin="0,0,4,4"/>
            <Button Content="Contabilidade" Style="{StaticResource BtnGhost}" Command="{Binding TagContabilidadeCommand}" Margin="0,0,4,4"/>
            <Button Content="Inovafarma" Style="{StaticResource BtnGhost}" Command="{Binding MoveToInovafarmaCommand}" Margin="0,0,4,4"/>
            <Button Content="Hiper" Style="{StaticResource BtnGhost}" Command="{Binding MoveToHiperCommand}" Margin="0,0,4,4"/>
            <Button Content="Pasta lida" Style="{StaticResource BtnGhost}" Command="{Binding MarkFolderReadCommand}" Margin="0,0,4,4"/>
            <Button Content="Esvaziar lixo" Style="{StaticResource BtnGhost}" Command="{Binding EmptyTrashCommand}" Margin="0,0,4,4"/>
            <Button Content="Limpar news" Style="{StaticResource BtnGhost}" Command="{Binding CleanupNewslettersCommand}" Margin="0,0,4,4"/>
          </WrapPanel>
        </StackPanel>
        <ListBox ItemsSource="{Binding Messages}" SelectedItem="{Binding SelectedMessage}" BorderThickness="0"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 ScrollViewer.CanContentScroll="True">
          <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
              <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
              <Setter Property="Padding" Value="0"/>
              <Setter Property="Foreground" Value="{DynamicResource Text}"/>
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="MsgBd" Padding="0" Background="Transparent">
                      <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsSelected" Value="True">
                        <Setter TargetName="MsgBd" Property="Background" Value="{DynamicResource ListSelected}"/>
                      </Trigger>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="MsgBd" Property="Background" Value="{DynamicResource ListHover}"/>
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </ListBox.ItemContainerStyle>
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Border Padding="8,6">
                <StackPanel>
                  <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Text="{Binding WhenLabel}" Foreground="{DynamicResource Muted}" FontSize="11"/>
                    <TextBlock Text="{Binding DisplayFrom}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                  </DockPanel>
                  <TextBlock Text="{Binding Subject}" TextTrimming="CharacterEllipsis"/>
                  <TextBlock Text="{Binding Preview}" Foreground="{DynamicResource Muted}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
              </Border>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </DockPanel>
      <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch" Background="{DynamicResource Line}"/>
      <DockPanel Grid.Column="4" Background="{DynamicResource ReaderBg}">
        <Border DockPanel.Dock="Top" Padding="12" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1">
          <StackPanel>
            <TextBlock Text="{Binding SubjectLine}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding FromLine}" Foreground="{DynamicResource Muted}" Margin="0,4,0,8"/>
            <StackPanel Orientation="Horizontal">
              <Button Content="Responder" Style="{StaticResource BtnPrimary}" Command="{Binding ReplyCommand}" Margin="0,0,6,0"/>
              <Button Content="Encaminhar" Style="{StaticResource BtnGhost}" Command="{Binding ForwardCommand}" Margin="0,0,6,0"/>
              <Button Content="Anexos" Style="{StaticResource BtnGhost}" Command="{Binding SaveAttachmentCommand}" Margin="0,0,6,0"/>
              <Button Content="Prioridade" Style="{StaticResource BtnGhost}" Command="{Binding ToggleFlagCommand}"/>
            </StackPanel>
          </StackPanel>
        </Border>
        <wv2:WebView2 x:Name="Reader"/>
      </DockPanel>
    </Grid>
    </DockPanel>
    <Border Background="{DynamicResource OverlayScrim}" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" IsHitTestVisible="True">
      <Border Width="320" Padding="20" CornerRadius="16" Background="{DynamicResource CardBg}" VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel>
          <TextBlock Text="Sincronizando caixa…" FontWeight="SemiBold" Foreground="{DynamicResource Text}" Margin="0,0,0,10"/>
          <ProgressBar IsIndeterminate="True" Height="8"/>
        </StackPanel>
      </Border>
    </Border>
  </Grid>
</Window>
